# TP-003: 이웃 돌봄 매칭 시스템 테스트 계획

- **기능 명세**: FEAT-003 (Neighbor Care Matching)
- **작성일**: 2026-03-14
- **우선순위**: P1 (중요) - 서비스의 사회적 안전망 확장 핵심 기능
- **테스트 범위**: 자원봉사자 등록/승인, 돌봄 수신 동의, 매칭 알고리즘(Haversine 거리 계산), 방문 보고서, 신뢰 점수, Kafka 이벤트 흐름, 보안, 성능

---

## 1. 자원봉사자 등록

### TC-003-001: 자원봉사자 등록 신청 (정상)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 사용자 로그인 완료 (JWT 발급, ROLE_USER), 자원봉사자 미등록 상태
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/volunteers/register` 호출, body: `{"activityRadius": 2.0, "activityAddress": "서울시 강남구 역삼동", "activityLatitude": 37.5000, "activityLongitude": 127.0360, "availableTimeStart": "09:00", "availableTimeEnd": "18:00", "availableDays": ["MON","WED","FRI","SAT"], "phoneNumber": "010-1234-5678", "introduction": "근처에 사는 직장인입니다."}` | 201 CREATED, `status: "PENDING"`, `volunteerId` 반환 | |
| 2 | volunteers 테이블 확인 | `status = PENDING`, `trust_score = 50`, `total_visits = 0` | |
| 3 | `GET /api/volunteers/me` 호출 | 200 OK, 등록된 정보와 일치 | |

- **테스트 데이터**: 일반 사용자 계정 (ROLE_USER)

### TC-003-002: 자원봉사자 중복 등록 방지
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 이미 자원봉사자로 등록 신청한 사용자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/volunteers/register` 재호출 (동일 사용자) | 409 CONFLICT, "이미 자원봉사자로 등록되어 있습니다" | |
| 2 | volunteers 테이블 확인 | 해당 user_id로 1건만 존재 (UNIQUE 제약 유지) | |

### TC-003-003: 자원봉사자 등록 - 필수 필드 유효성 검증
- **우선순위**: P0
- **유형**: Unit / Integration
- **사전 조건**: 사용자 로그인 완료
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `activityAddress` 누락 | 400 BAD_REQUEST | |
| 2 | `activityLatitude` 누락 | 400 BAD_REQUEST | |
| 3 | `activityLongitude` 누락 | 400 BAD_REQUEST | |
| 4 | `availableTimeStart` 누락 | 400 BAD_REQUEST | |
| 5 | `availableTimeEnd` 누락 | 400 BAD_REQUEST | |
| 6 | `availableDays: []` (빈 배열) | 400 BAD_REQUEST | |
| 7 | `phoneNumber` 누락 | 400 BAD_REQUEST | |
| 8 | `phoneNumber: "invalid"` (형식 오류) | 400 BAD_REQUEST | |
| 9 | `availableTimeStart: "18:00"`, `availableTimeEnd: "09:00"` (시작 > 종료) | 400 BAD_REQUEST | |
| 10 | `activityRadius: -1.0` (음수) | 400 BAD_REQUEST | |
| 11 | `activityLatitude: 200.0` (범위 초과) | 400 BAD_REQUEST | |
| 12 | `activityLongitude: 200.0` (범위 초과) | 400 BAD_REQUEST | |

### TC-003-004: 자원봉사자 정보 수정
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 자원봉사자 등록 완료 (PENDING 또는 ACTIVE 상태)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `PUT /api/volunteers/me` 호출, `activityRadius: 3.0` 변경 | 200 OK | |
| 2 | `PUT /api/volunteers/me` 호출, `availableDays: ["SAT","SUN"]` 변경 | 200 OK | |
| 3 | `GET /api/volunteers/me` 호출 | 변경된 값으로 반환 | |

---

## 2. 자원봉사자 상태 관리

### TC-003-005: 자원봉사자 상태 전이 - PENDING -> APPROVED(ACTIVE)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: PENDING 상태 자원봉사자 존재, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `PUT /api/admin/volunteers/{volunteerId}/approve` 호출 | 200 OK, "자원봉사자가 승인되었습니다" | |
| 2 | volunteers 테이블 확인 | `status = ACTIVE`, `approved_at` 설정됨, `approved_by`에 관리자 정보 | |
| 3 | 자원봉사자 `GET /api/volunteers/me` 확인 | `status: "ACTIVE"` | |

### TC-003-006: 자원봉사자 상태 전이 - PENDING -> REJECTED
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: PENDING 상태 자원봉사자 존재, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `PUT /api/admin/volunteers/{volunteerId}/reject` 호출, `reason: "활동 지역 정보가 불충분합니다."` | 200 OK, "자원봉사자 신청이 반려되었습니다" | |
| 2 | volunteers 테이블 확인 | `status = REJECTED`, `rejected_reason` 저장됨 | |

### TC-003-007: 신뢰 점수 30점 미만 시 자동 자격 정지 (ACTIVE -> SUSPENDED)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 상태 자원봉사자, `trust_score = 32`
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 부정 평가 (1~2점) 발생하여 trust_score -5 적용 -> 27점 | 자원봉사자 status가 SUSPENDED로 자동 변경 | |
| 2 | volunteers 테이블 확인 | `status = SUSPENDED`, `trust_score = 27` | |
| 3 | `GET /api/volunteers/me` 확인 | `status: "SUSPENDED"` | |
| 4 | 해당 자원봉사자의 ACTIVE 매칭 확인 | 담당 대상자 재매칭 필요 알림이 관리자에게 발송됨 | |

### TC-003-008: 정지된 자원봉사자 수동 복구 (SUSPENDED -> ACTIVE)
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: SUSPENDED 상태 자원봉사자, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 관리자가 자원봉사자 상태를 ACTIVE로 수동 복구 | 200 OK | |
| 2 | volunteers 테이블 확인 | `status = ACTIVE` | |

### TC-003-009: REJECTED 상태 자원봉사자 승인 시도
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: REJECTED 상태 자원봉사자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `PUT /api/admin/volunteers/{volunteerId}/approve` 호출 | 400 BAD_REQUEST 또는 409 CONFLICT, 잘못된 상태 전이 | |

### TC-003-010: 미승인(PENDING) 자원봉사자의 매칭/방문 시도 차단
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: PENDING 상태 자원봉사자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `GET /api/volunteers/me/matches` 호출 | 빈 목록 또는 403 Forbidden (매칭 대상 아님) | |
| 2 | `POST /api/care-visits` 방문 보고서 작성 시도 | 403 Forbidden, "승인된 자원봉사자만 활동 가능합니다" | |

---

## 3. 관리자 승인 워크플로우

### TC-003-011: 관리자 자원봉사자 목록 조회 (상태별 필터)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 다양한 상태의 자원봉사자 데이터 존재, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `GET /api/admin/volunteers` (전체) | 200 OK, 모든 자원봉사자 목록 반환, 페이징 정보 포함 | |
| 2 | `GET /api/admin/volunteers?status=PENDING` | PENDING 상태만 반환 | |
| 3 | `GET /api/admin/volunteers?status=ACTIVE` | ACTIVE 상태만 반환 | |
| 4 | `GET /api/admin/volunteers?status=SUSPENDED` | SUSPENDED 상태만 반환 | |
| 5 | `GET /api/admin/volunteers?page=0&size=5` | 5건씩 페이징 | |

### TC-003-012: 자원봉사자 반려 시 사유 누락
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: PENDING 상태 자원봉사자, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `PUT /api/admin/volunteers/{volunteerId}/reject` 호출, `reason` 필드 누락 | 400 BAD_REQUEST, 반려 사유 필수 | |
| 2 | `PUT /api/admin/volunteers/{volunteerId}/reject` 호출, `reason: ""` (빈 문자열) | 400 BAD_REQUEST | |

---

## 4. 돌봄 수신 동의

### TC-003-013: 돌봄 서비스 수신 동의 설정 (정상)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 사용자 로그인 완료, 주소 정보 등록 완료
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/users/me/care-consent` 호출, body: `{"consentGiven": true, "allowedVisitTimeStart": "10:00", "allowedVisitTimeEnd": "20:00", "allowedVisitDays": ["MON","TUE","WED","THU","FRI"], "shareNameLevel": "LAST_NAME_ONLY", "shareAddressLevel": "DONG_ONLY", "sharePhone": false, "visitFrequency": "WEEKLY"}` | 200 OK, `careConsentId` 반환, `consentGiven: true` | |
| 2 | care_consents 테이블 확인 | 레코드 생성됨, `consented_at` 설정 | |

### TC-003-014: 돌봄 서비스 수신 동의 철회
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 돌봄 수신 동의 활성 상태
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/users/me/care-consent` 호출, `consentGiven: false` | 200 OK | |
| 2 | 기존 ACTIVE 매칭 확인 | 매칭이 TERMINATED 상태로 변경됨 | |

### TC-003-015: 주소 미등록 사용자의 돌봄 동의 시도
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 사용자 프로필에 주소 정보 미등록
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/users/me/care-consent` 호출, `consentGiven: true` | 400 BAD_REQUEST, "주소 정보를 먼저 등록해주세요" | |

---

## 5. 인근 자원봉사자 탐색 (Haversine 거리 계산)

### TC-003-016: 반경 2km 이내 자원봉사자 정상 탐색
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 대상자 위치 (37.5000, 127.0360), 반경 2km 이내에 ACTIVE 자원봉사자 3명 존재
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 알고리즘 실행 (대상자 기준 2km 이내 탐색) | 3명의 자원봉사자 후보 반환 | |
| 2 | 반환 결과의 거리 값 확인 | 모든 후보의 `distanceKm < 2.0` | |
| 3 | 정렬 순서 확인 | 거리가 가까운 순으로 정렬 (추가: 시간대 겹침, 신뢰 점수, 담당 수) | |

### TC-003-017: Haversine 거리 계산 정확성 검증
- **우선순위**: P0
- **유형**: Unit
- **사전 조건**: 알려진 좌표 쌍으로 거리 계산
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 서울역 (37.5547, 126.9707) ~ 강남역 (37.4979, 127.0276) 거리 계산 | 약 8.0km (오차 +/- 0.5km 이내) | |
| 2 | 동일 좌표 간 거리 계산 | 0.0km | |
| 3 | 위도 37.5000, 경도 127.0360 ~ 위도 37.5100, 경도 127.0360 (위도만 0.01 차이) | 약 1.1km (오차 +/- 0.1km 이내) | |

### TC-003-018: 반경 2km 이내 자원봉사자 없음 -> 5km 확대
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 대상자 반경 2km 이내 ACTIVE 자원봉사자 0명, 반경 5km 이내 2명 존재
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 알고리즘 실행 (2km 탐색) | 후보 0명 | |
| 2 | 자동 반경 확대 (5km 재탐색) | 2명의 자원봉사자 후보 반환, `distanceKm` 값이 2.0 < d <= 5.0 | |

### TC-003-019: 반경 5km 이내에도 자원봉사자 없음
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 대상자 반경 5km 이내 ACTIVE 자원봉사자 0명
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 알고리즘 실행 (2km -> 5km 확대 탐색) | 후보 0명 | |
| 2 | 관리자에게 알림 발송 확인 | "해당 지역에 활동 가능한 자원봉사자가 없습니다" 알림 | |
| 3 | 대상자에게 안내 메시지 발송 | "현재 지역에 활동 가능한 자원봉사자를 찾고 있습니다" | |

### TC-003-020: 정확히 반경 2km 경계에 위치한 자원봉사자
- **우선순위**: P1
- **유형**: Unit / Integration
- **사전 조건**: 대상자로부터 Haversine 거리가 정확히 2.000km인 자원봉사자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 알고리즘 실행 (2km 이내 탐색) | 경계값 포함 여부 일관성 확인 (2km 이내 = d <= 2.0) | |
| 2 | 2.001km 자원봉사자 | 2km 탐색에서 제외, 5km 확대 시 포함 | |

### TC-003-021: 위치 좌표 (0, 0) 처리
- **우선순위**: P2
- **유형**: Unit / Integration
- **사전 조건**: 자원봉사자 또는 대상자의 좌표가 (0, 0)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 좌표 (0, 0)인 자원봉사자로 매칭 알고리즘 실행 | 해당 자원봉사자 매칭 대상에서 제외되거나, 유효성 검증에서 거부됨 | |
| 2 | 좌표 (0, 0)인 대상자로 매칭 시도 | 유효한 주소 등록 안내 메시지 반환 | |

### TC-003-022: 매칭 우선순위 정렬 검증
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 반경 내 ACTIVE 자원봉사자 5명 (거리, 시간대, 신뢰 점수, 담당 수 다양)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 알고리즘 실행 | 거리 > 시간대 겹침 > 신뢰 점수 > 담당 수 순서로 가중치 적용된 정렬 | |
| 2 | 거리가 동일한 경우 신뢰 점수가 높은 자원봉사자 우선 | 정렬 순서 일치 | |
| 3 | 담당 대상자 5명인 자원봉사자 | 후보에서 제외됨 | |

---

## 6. 돌봄 매칭 생성

### TC-003-023: 매칭 생성 (정상)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 자원봉사자 + 돌봄 수신 동의한 대상자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 후보 생성 후 관리자 확인 -> 매칭 확정 | care_matches 레코드 생성, `status = PENDING -> ACTIVE` | |
| 2 | 자원봉사자에게 매칭 알림 발송 확인 | 알림 수신됨 | |
| 3 | 대상자에게 매칭 알림 발송 확인 | 알림 수신됨 | |
| 4 | `GET /api/volunteers/me/matches` 확인 | 매칭 정보 포함, 대상자 정보는 동의 범위 내 | |
| 5 | `distance_km` 값 확인 | Haversine 거리 계산 값과 일치 | |

### TC-003-024: 자원봉사자 최대 담당 5명 제한
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 자원봉사자가 이미 5개의 ACTIVE 매칭 보유
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 해당 자원봉사자로 6번째 매칭 생성 시도 | 거부됨, "최대 담당 인원(5명)을 초과할 수 없습니다" | |
| 2 | 매칭 후보 탐색 시 해당 자원봉사자 | 담당 5명인 자원봉사자는 후보에서 자동 제외 | |

### TC-003-025: 자기 자신과의 매칭 방지
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 자원봉사자이면서 동시에 돌봄 수신 동의한 사용자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 알고리즘에서 자기 자신이 대상자로 탐색될 경우 | 매칭 후보에서 제외됨 | |
| 2 | 수동으로 동일 user_id 자원봉사자-대상자 매칭 생성 시도 | 거부됨, "자기 자신과 매칭할 수 없습니다" | |

### TC-003-026: 동일 자원봉사자-대상자 간 중복 매칭 방지
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 자원봉사자 A와 대상자 B의 ACTIVE 매칭 존재
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 동일 자원봉사자 A - 대상자 B 매칭 재생성 시도 | 409 CONFLICT, UNIQUE 제약 위반 또는 비즈니스 로직 거부 | |

### TC-003-027: 동일 대상자에 대한 동시 매칭 요청 (Race Condition)
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 대상자 C에 대해 자원봉사자 A, B가 거의 동시에 매칭 요청
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 2개의 매칭 생성 요청을 동시 발행 | 하나만 성공, 나머지는 거부 또는 대기 (데이터 정합성 유지) | |
| 2 | care_matches 테이블 확인 | 동일 자원봉사자-대상자 쌍은 1건만 존재 | |

---

## 7. 매칭 상태 전이 (Lifecycle)

### TC-003-028: 매칭 상태 전이 - PENDING -> ACTIVE
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: PENDING 매칭, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 관리자가 매칭 확정 | `status = ACTIVE`, `matched_at` 기록 | |
| 2 | 정기 방문 일정 자동 생성 확인 | `next_scheduled_visit`이 `visit_frequency`에 따라 설정됨 | |

### TC-003-029: 매칭 상태 전이 - ACTIVE -> TERMINATED (관리자 수동)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 관리자가 매칭 종료 처리, `terminationReason: "대상자 요청"` | `status = TERMINATED`, `terminated_at`, `termination_reason` 기록 | |
| 2 | 자원봉사자의 `currentMatchCount` 확인 | 1 감소 | |

### TC-003-030: 매칭 상태 전이 - ACTIVE -> TERMINATED (대상자 동의 철회)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭, 대상자가 돌봄 수신 동의 철회
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 대상자가 `POST /api/users/me/care-consent` 호출, `consentGiven: false` | 해당 대상자의 모든 ACTIVE 매칭이 TERMINATED로 변경 | |
| 2 | 자원봉사자에게 매칭 종료 알림 발송 | 알림 수신됨 | |

### TC-003-031: 이미 TERMINATED 매칭에 대한 재종료 시도
- **우선순위**: P2
- **유형**: Integration
- **사전 조건**: TERMINATED 상태 매칭
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 관리자가 해당 매칭 종료 재시도 | 400 BAD_REQUEST, "이미 종료된 매칭입니다" | |

---

## 8. 방문 일정 관리

### TC-003-032: 정기 방문 일정 자동 생성
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭, `visitFrequency: WEEKLY`
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 ACTIVE 확정 후 `next_scheduled_visit` 확인 | 대상자의 방문 허용 시간대 내에서 다음 주 일정 생성 | |
| 2 | 방문 완료 보고서 제출 후 `next_scheduled_visit` 확인 | 다음 주 일정으로 자동 갱신 | |

### TC-003-033: 과거 날짜로 방문 일정 생성 시도
- **우선순위**: P1
- **유형**: Unit / Integration
- **사전 조건**: ACTIVE 매칭
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `visitedAt`이 미래 시각 (아직 오지 않은 날짜) | 400 BAD_REQUEST, "방문 시간은 미래일 수 없습니다" 또는 보고서의 visitedAt이 과거/현재만 허용 | |
| 2 | `visitedAt`이 매칭 시작(`matched_at`) 이전 날짜 | 400 BAD_REQUEST, "매칭 시작 이전의 방문을 기록할 수 없습니다" | |

### TC-003-034: 방문 일정 충돌 감지
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 자원봉사자가 동일 시간대에 2명의 대상자 방문 예정
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 동일 시간에 겹치는 방문 일정 생성 시도 | 경고 또는 충돌 알림 발생 | |
| 2 | 자원봉사자에게 일정 조정 안내 | 충돌 정보 포함 알림 | |

---

## 9. 방문 보고서 제출

### TC-003-035: 방문 보고서 작성 (정상 - 양호)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭, 자원봉사자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/care-visits` 호출, `matchId: 1, visitType: "REGULAR", visitedAt: "2026-03-14T14:30:00", duration: 30, receiverStatus: "GOOD", notes: "건강 상태 양호"` | 201 CREATED, `visitId` 반환 | |
| 2 | care_visits 테이블 확인 | 레코드 정상 생성 | |
| 3 | care_matches의 `total_visits` 확인 | 1 증가 | |
| 4 | volunteers의 `total_visits` 확인 | 1 증가 | |
| 5 | volunteers의 `trust_score` 확인 | +2점 (방문 완료 보너스) | |

### TC-003-036: 방문 보고서 - 대상자 부재 (에스컬레이션 트리거)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/care-visits` 호출, `receiverStatus: "ABSENT"` | 201 CREATED | |
| 2 | 에스컬레이션 상향 트리거 확인 | 해당 대상자의 에스컬레이션 레벨 상향됨 | |
| 3 | 보호자에게 즉시 알림 발송 확인 | "자원봉사자 방문 시 부재" 알림 | |

### TC-003-037: 방문 보고서 - 대상자 거부
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/care-visits` 호출, `receiverStatus: "REFUSED", refusedByReceiver: true` | 201 CREATED | |
| 2 | 자원봉사자 신뢰 점수 확인 | 감점 없음 (방문 거부 시) | |
| 3 | 연속 3회 거부 시 | 관리자에게 알림 발송, 서비스 수신 동의 재확인 요청 | |

### TC-003-038: 방문 보고서 - 위험 상태
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/care-visits` 호출, `receiverStatus: "DANGER"` | 201 CREATED | |
| 2 | 관리자에게 긴급 알림 발송 확인 | 위험 상태 알림 수신됨 | |
| 3 | 에스컬레이션 상향 트리거 확인 | DANGER 레벨 에스컬레이션 진입 | |

### TC-003-039: 보고서 24시간 내 미제출 시 신뢰 점수 감점
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 방문 예정 시간 경과 후 24시간 초과, 보고서 미제출
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 배치/스케줄러 실행 (24시간 초과 미제출 감지) | 해당 자원봉사자 `trust_score` -3점 적용 | |
| 2 | volunteers 테이블 확인 | trust_score 감소 반영 | |

### TC-003-040: 존재하지 않는 매칭에 대한 보고서 작성 시도
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 유효하지 않은 matchId
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/care-visits` 호출, `matchId: 99999` (존재하지 않음) | 404 NOT_FOUND | |

### TC-003-041: 다른 자원봉사자의 매칭에 대한 보고서 작성 시도
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 자원봉사자 A의 매칭에 대해 자원봉사자 B가 보고서 작성 시도
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 자원봉사자 B가 `POST /api/care-visits`, `matchId: (A의 매칭 ID)` | 403 Forbidden, "본인의 매칭에 대해서만 보고서를 작성할 수 있습니다" | |

---

## 10. 방문 평가

### TC-003-042: 대상자의 방문 평가 (정상)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 방문 보고서 제출 완료, 대상자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `POST /api/care-visits/{visitId}/rate` 호출, `rating: 5, comment: "친절하게 방문해 주셨습니다"` | 200 OK | |
| 2 | care_visits 테이블 확인 | `rating = 5`, `rating_comment` 저장됨 | |
| 3 | volunteers의 `total_rating_sum`, `total_rating_count` 확인 | 각각 +5, +1 증가 | |

### TC-003-043: 긍정 평가 (4~5점) 시 신뢰 점수 +3
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 방문 보고서 제출 완료, 자원봉사자 trust_score = 50
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 대상자가 4점 평가 | 자원봉사자 trust_score = 53 (+2 방문 완료 + 추가 없음, 또는 이미 +2 적용 후 +3 적용 = 55) | |
| 2 | 정확한 점수 확인 | 방문 완료 +2점은 보고서 제출 시 적용, 긍정 평가 +3점은 평가 시 적용, 합산 trust_score = 55 | |

### TC-003-044: 부정 평가 (1~2점) 시 신뢰 점수 -5
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 방문 보고서 제출 완료, 자원봉사자 trust_score = 50
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 대상자가 1점 평가 | 자원봉사자 trust_score = 47 (방문 +2, 부정 평가 -5) | |
| 2 | volunteers 테이블 확인 | `trust_score = 47` | |

### TC-003-045: 평가 범위 유효성 검증
- **우선순위**: P1
- **유형**: Unit / Integration
- **사전 조건**: 방문 보고서 제출 완료
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `rating: 0` | 400 BAD_REQUEST, "평가 점수는 1~5점이어야 합니다" | |
| 2 | `rating: 6` | 400 BAD_REQUEST | |
| 3 | `rating: -1` | 400 BAD_REQUEST | |

### TC-003-046: 본인이 아닌 대상자의 방문 평가 시도
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 방문 보고서의 대상자가 아닌 다른 사용자
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 다른 사용자가 `POST /api/care-visits/{visitId}/rate` 호출 | 403 Forbidden | |

---

## 11. 신뢰 점수 계산

### TC-003-047: 신뢰 점수 초기값 검증
- **우선순위**: P0
- **유형**: Unit
- **사전 조건**: 신규 자원봉사자 등록
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 자원봉사자 등록 완료 | `trust_score = 50` | |

### TC-003-048: 방문 미이행 시 신뢰 점수 -10
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 방문 예정이었으나 방문 미이행 (no-show)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 방문 미이행 감지 (스케줄러/배치) | trust_score -10 적용 | |
| 2 | trust_score = 35인 자원봉사자가 미이행 | trust_score = 25, 30점 미만이므로 자동 SUSPENDED | |

### TC-003-049: 신뢰 점수 경계값 - 정확히 30점
- **우선순위**: P0
- **유형**: Unit / Integration
- **사전 조건**: 자원봉사자 trust_score = 30
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | trust_score가 정확히 30 유지 | ACTIVE 상태 유지 (30점 미만 시 정지이므로 30점은 정상) | |
| 2 | trust_score가 30 -> 29 감소 | SUSPENDED로 자동 전환 | |

### TC-003-050: 신뢰 점수 상한/하한 경계
- **우선순위**: P1
- **유형**: Unit
- **사전 조건**: 다양한 trust_score 값
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | trust_score = 99, 방문 완료 (+2) | trust_score = 100 (최대 100 제한) | |
| 2 | trust_score = 100, 긍정 평가 (+3) | trust_score = 100 (100 초과 불가) | |
| 3 | trust_score = 3, 부정 평가 (-5) | trust_score = 0 (최소 0 제한), SUSPENDED | |
| 4 | trust_score = 0, 추가 감점 | trust_score = 0 (0 미만 불가) | |

### TC-003-051: 자격 정지 후 복구, 재활동 시 점수 누적
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: SUSPENDED 자원봉사자 (trust_score = 25), 관리자 복구
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 관리자가 ACTIVE로 복구 | status = ACTIVE, trust_score는 25 유지 (리셋하지 않음) | |
| 2 | 방문 완료 (+2) | trust_score = 27 | |
| 3 | 긍정 평가 (+3) | trust_score = 30, ACTIVE 유지 | |

---

## 12. 자원봉사자 탈퇴/활동 중단 시 매칭 처리

### TC-003-052: 자원봉사자 활동 중단 시 ACTIVE 매칭 처리
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: ACTIVE 매칭 3건 보유한 자원봉사자가 활동 중단 (WITHDRAWN 또는 SUSPENDED)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 자원봉사자가 자격 정지(SUSPENDED)됨 | 해당 자원봉사자의 모든 ACTIVE 매칭이 TERMINATED로 변경 | |
| 2 | 각 대상자에게 알림 발송 | "자원봉사자 변경 예정" 안내 알림 | |
| 3 | 관리자에게 재매칭 필요 알림 | 재매칭 대상 목록 표시 | |

---

## 13. Kafka 이벤트 통합 테스트

### TC-003-053: 매칭 생성 -> Kafka -> Event Service -> MongoDB
- **우선순위**: P0
- **유형**: Integration (E2E)
- **사전 조건**: Docker Compose 환경 기동 (Kafka, PostgreSQL, MongoDB 포함)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 매칭 생성 확정 | care-match-events 토픽에 `eventType: MATCH_CREATED` 이벤트 발행 | |
| 2 | Event Service가 care-match-events 소비 | MongoDB care_visit_logs 컬렉션에 매칭 이벤트 로그 기록 | |
| 3 | 이벤트 데이터 확인 | `matchId`, `volunteerId`, `receiverUserId`, `timestamp` 일치 | |

### TC-003-054: 방문 보고서 제출 -> Kafka -> Event Service -> MongoDB
- **우선순위**: P0
- **유형**: Integration (E2E)
- **사전 조건**: Docker Compose 환경 기동
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 방문 보고서 제출 | care-visit-events 토픽에 `eventType: VISIT_COMPLETED` 이벤트 발행 | |
| 2 | Event Service가 care-visit-events 소비 | MongoDB에 방문 이벤트 로그 기록 | |
| 3 | metadata 확인 | `distanceKm`, `duration`, `escalationTriggered` 값 정확 | |

### TC-003-055: 방문 보고서(ABSENT) -> 신뢰 점수 갱신 -> 자동 정지 흐름
- **우선순위**: P0
- **유형**: Integration (E2E)
- **사전 조건**: trust_score = 38 자원봉사자, 방문 미이행 감지
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 방문 미이행 감지 (-10) | trust_score = 28, 30점 미만 | |
| 2 | 자원봉사자 자동 SUSPENDED | volunteers.status = SUSPENDED | |
| 3 | ACTIVE 매칭 자동 TERMINATED | 모든 매칭 종료 | |
| 4 | Kafka 이벤트 발행 확인 | care-match-events에 MATCH_TERMINATED 이벤트들 발행 | |
| 5 | MongoDB 로그 확인 | 정지 및 매칭 종료 이벤트 기록 | |

### TC-003-056: 관리자 승인 -> 자원봉사자 매칭 가능 상태 확인
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: PENDING 자원봉사자, 반경 내 돌봄 수신 동의 대상자 존재
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 관리자가 자원봉사자 승인 (PENDING -> ACTIVE) | status = ACTIVE | |
| 2 | 매칭 알고리즘에서 해당 자원봉사자 탐색 | 매칭 후보 목록에 포함됨 | |
| 3 | 승인 전 매칭 후보 탐색 시 | 해당 자원봉사자 포함되지 않음 (PENDING은 제외) | |

### TC-003-057: Kafka 장애 시 방문 보고서 처리
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: Kafka 브로커 일시 중단
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | Kafka 중단 상태에서 방문 보고서 제출 | PostgreSQL에 정상 저장, Kafka 발행은 재시도 큐에 보관 | |
| 2 | Kafka 복구 후 | 보관된 이벤트가 순서대로 발행됨 | |

---

## 14. 보안 테스트

### TC-003-058: 사용자 API 인증 검증 (자원봉사자 관련)
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: N/A
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | JWT 없이 `POST /api/volunteers/register` 호출 | 401 Unauthorized | |
| 2 | JWT 없이 `GET /api/volunteers/me` 호출 | 401 Unauthorized | |
| 3 | JWT 없이 `GET /api/volunteers/me/matches` 호출 | 401 Unauthorized | |
| 4 | JWT 없이 `POST /api/care-visits` 호출 | 401 Unauthorized | |
| 5 | JWT 없이 `POST /api/care-visits/{visitId}/rate` 호출 | 401 Unauthorized | |
| 6 | JWT 없이 `POST /api/users/me/care-consent` 호출 | 401 Unauthorized | |
| 7 | 만료된 JWT로 호출 | 401 Unauthorized | |
| 8 | 유효한 JWT로 호출 | 200 OK 또는 201 CREATED | |

### TC-003-059: 사용자 데이터 접근 격리
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 사용자 A, B 각각 자원봉사자 등록
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 사용자 A의 JWT로 `GET /api/volunteers/me` | 사용자 A의 자원봉사자 정보만 반환 | |
| 2 | 사용자 A의 JWT로 사용자 B의 매칭 목록 접근 시도 | 자기 자신의 매칭만 반환 (다른 사용자 매칭 미노출) | |
| 3 | 대상자의 상세 주소가 매칭 전 자원봉사자에게 노출되지 않음 | 동의 범위(DONG_ONLY) 내 정보만 반환 | |
| 4 | 매칭 확정 후 자원봉사자가 대상자 정보 조회 | 동의 범위 내 정보만 반환 (상세 주소는 방문 수락 후만 공개) | |

### TC-003-060: 관리자 전용 API 보호
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: N/A
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | Basic Auth 없이 `GET /api/admin/volunteers` 호출 | 401 Unauthorized | |
| 2 | 일반 사용자 JWT로 `GET /api/admin/volunteers` 호출 | 403 Forbidden | |
| 3 | 일반 사용자 JWT로 `PUT /api/admin/volunteers/{id}/approve` 호출 | 403 Forbidden | |
| 4 | 일반 사용자 JWT로 `GET /api/admin/care-matches` 호출 | 403 Forbidden | |
| 5 | 일반 사용자 JWT로 `GET /api/admin/care-visits` 호출 | 403 Forbidden | |
| 6 | 관리자 Basic Auth로 호출 | 200 OK | |

### TC-003-061: 개인정보 공유 범위 준수 검증
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 대상자가 `shareNameLevel: LAST_NAME_ONLY`, `shareAddressLevel: DONG_ONLY`, `sharePhone: false` 설정
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 자원봉사자가 매칭 목록 조회 | `displayName`이 "김*" 형태 (성만 표시) | |
| 2 | `areaName` 확인 | "역삼동" (동 단위만 표시) | |
| 3 | 전화번호 미노출 확인 | 전화번호 필드 없음 또는 null | |
| 4 | 방문 수락 후 상세 주소 확인 | 호수 단위까지 공개 (방문 수락 후만) | |

---

## 15. 관리자 조회 API

### TC-003-062: 관리자 매칭 현황 조회
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 다양한 상태의 매칭 데이터 존재, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `GET /api/admin/care-matches` (전체) | 200 OK, 모든 매칭 목록 반환, 페이징 정보 포함 | |
| 2 | `GET /api/admin/care-matches?status=ACTIVE` | ACTIVE 매칭만 반환 | |
| 3 | `GET /api/admin/care-matches?status=PENDING` | PENDING 매칭만 반환 | |
| 4 | `GET /api/admin/care-matches?status=TERMINATED` | TERMINATED 매칭만 반환 | |

### TC-003-063: 관리자 방문 보고서 조회
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 다양한 상태의 방문 보고서 존재, 관리자 로그인
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `GET /api/admin/care-visits` (전체) | 200 OK, 방문 보고서 목록 반환, 페이징 포함 | |
| 2 | `GET /api/admin/care-visits?receiverStatus=DANGER` | DANGER 상태 보고서만 반환 | |
| 3 | `GET /api/admin/care-visits?receiverStatus=ABSENT` | ABSENT 상태 보고서만 반환 | |

---

## 16. 성능 테스트

### TC-003-064: 인근 자원봉사자 탐색 - 10K+ 자원봉사자
- **우선순위**: P1
- **유형**: Performance
- **사전 조건**: 10,000명 이상의 ACTIVE 자원봉사자 데이터 준비 (다양한 위치 분포)
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 반경 2km 이내 자원봉사자 탐색 쿼리 실행 | 응답 시간 < 500ms | |
| 2 | 반경 5km 이내 자원봉사자 탐색 쿼리 실행 | 응답 시간 < 1초 | |
| 3 | 인덱스 활용 확인 (EXPLAIN ANALYZE) | `idx_volunteers_location` 인덱스 사용됨 | |
| 4 | 메모리 사용량 확인 | OOM 미발생 | |

### TC-003-065: 동시 매칭 생성 성능
- **우선순위**: P1
- **유형**: Performance
- **사전 조건**: 100명의 대상자에 대해 동시 매칭 요청
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | 100건의 매칭 생성 동시 요청 | 모든 요청 30초 이내 처리 완료 | |
| 2 | UNIQUE 제약 위반 없이 정상 처리 확인 | 중복 매칭 없음, 데이터 정합성 유지 | |
| 3 | 자원봉사자 최대 담당 수 제한 정합성 | 5명 초과 할당 없음 | |

### TC-003-066: 관리자 대시보드 응답 시간 (대량 데이터)
- **우선순위**: P2
- **유형**: Performance
- **사전 조건**: 자원봉사자 1,000명, 매칭 5,000건, 방문 보고서 20,000건
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | `GET /api/admin/volunteers` 응답 시간 | < 2초 | |
| 2 | `GET /api/admin/care-matches` 응답 시간 | < 2초 | |
| 3 | `GET /api/admin/care-visits` 응답 시간 | < 2초 | |

---

## 17. 긴급 방문 요청 (에스컬레이션 연동)

### TC-003-067: 에스컬레이션 DANGER 시 매칭 자원봉사자에게 긴급 방문 요청
- **우선순위**: P0
- **유형**: Integration
- **사전 조건**: 대상자에게 ACTIVE 매칭된 자원봉사자 존재, 에스컬레이션 DANGER 발생
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | escalation-events 토픽에서 DANGER 이벤트 수신 | 매칭된 자원봉사자에게 긴급 방문 요청 알림 발송 | |
| 2 | 자원봉사자 30분 내 수락 | 긴급 방문 일정 생성, `visitType: EMERGENCY` | |
| 3 | 자원봉사자 30분 내 미응답 | 다음 순위 자원봉사자에게 요청 전환 | |
| 4 | 모든 자원봉사자 미응답 | 관리자에게 알림 발송 | |

### TC-003-068: 매칭 없는 대상자의 에스컬레이션 DANGER
- **우선순위**: P1
- **유형**: Integration
- **사전 조건**: 매칭된 자원봉사자가 없는 대상자, 에스컬레이션 DANGER
- **테스트 단계**:

| # | 입력/동작 | 기대 결과 | 통과 |
|---|----------|----------|------|
| 1 | escalation-events 수신 | 매칭 자원봉사자 없음 -> 관리자에게 직접 알림 발송 | |
| 2 | 반경 내 미매칭 자원봉사자에게 긴급 요청 (optional) | 가용한 자원봉사자에게 긴급 방문 요청 또는 관리자 판단 | |

---

## 테스트 환경 요구사항

| 항목 | 요구사항 |
|------|---------|
| Docker Compose | PostgreSQL 16, MongoDB 7, Kafka (Confluent 7.5.0), Zookeeper |
| 테스트 데이터 | 최소 50명 자원봉사자, 100명 대상자 (동의 완료), 다양한 위치 좌표 데이터 |
| 성능 테스트 | 10K+ 자원봉사자 시뮬레이션 데이터 (서울 지역 랜덤 좌표) |
| 외부 서비스 | SMS 서비스 Mock (알림 발송 검증), Kafka EmbeddedKafka (단위 테스트) |
| DB 확장 | PostgreSQL earthdistance 또는 PostGIS 확장 설치 필요 |
| 모니터링 | Prometheus + Grafana (위치 쿼리 성능, Kafka lag, API 응답 시간) |

---

## 테스트 케이스 요약

| 구분 | 케이스 수 | P0 | P1 | P2 |
|------|----------|----|----|-----|
| 자원봉사자 등록 | TC-003-001 ~ TC-003-004 | 3 | 1 | 0 |
| 자원봉사자 상태 관리 | TC-003-005 ~ TC-003-010 | 4 | 2 | 0 |
| 관리자 승인 워크플로우 | TC-003-011 ~ TC-003-012 | 1 | 1 | 0 |
| 돌봄 수신 동의 | TC-003-013 ~ TC-003-015 | 2 | 1 | 0 |
| 인근 자원봉사자 탐색 | TC-003-016 ~ TC-003-022 | 3 | 2 | 2 |
| 돌봄 매칭 생성 | TC-003-023 ~ TC-003-027 | 4 | 1 | 0 |
| 매칭 상태 전이 | TC-003-028 ~ TC-003-031 | 3 | 0 | 1 |
| 방문 일정 관리 | TC-003-032 ~ TC-003-034 | 1 | 2 | 0 |
| 방문 보고서 제출 | TC-003-035 ~ TC-003-041 | 3 | 3 | 0 |
| 방문 평가 | TC-003-042 ~ TC-003-046 | 3 | 1 | 0 |
| 신뢰 점수 계산 | TC-003-047 ~ TC-003-051 | 3 | 2 | 0 |
| 자원봉사자 탈퇴 처리 | TC-003-052 | 1 | 0 | 0 |
| Kafka 이벤트 통합 | TC-003-053 ~ TC-003-057 | 4 | 1 | 0 |
| 보안 | TC-003-058 ~ TC-003-061 | 4 | 0 | 0 |
| 관리자 조회 API | TC-003-062 ~ TC-003-063 | 2 | 0 | 0 |
| 성능 | TC-003-064 ~ TC-003-066 | 0 | 2 | 1 |
| 긴급 방문 요청 | TC-003-067 ~ TC-003-068 | 1 | 1 | 0 |
| **합계** | **68건** | **42** | **20** | **4** |
